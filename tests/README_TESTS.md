"""
ContractEx Test Suite

Run tests with:
    pytest                          # Run all tests
    pytest -m smoke                 # Quick smoke tests
    pytest -m unit                  # Unit tests only
    pytest -m integration           # Integration tests (requires API keys)
    pytest tests/test_models.py     # Specific test file
    pytest --cov=contractex         # With coverage report
"""

# Placeholder - tests will be implemented in Phase 2
# See NEXT_STEPS.md for testing priorities
